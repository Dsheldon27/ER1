void setup(){//setup function run once
   size(400,500);//sets window size
}
void draw(){//function runs over and ocer
  if(mousePressed){//conditional
  background(random(255),80,10,200);
  delay(1);//pause a sec
  }
}